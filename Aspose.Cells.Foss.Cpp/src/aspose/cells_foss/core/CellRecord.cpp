#include "aspose/cells_foss/core/CellRecord.h"

// All members of CellRecord are either defaulted or inline in the header.
// This translation unit exists to satisfy the build-system source list
// and to provide a place for future out-of-line definitions if needed.
